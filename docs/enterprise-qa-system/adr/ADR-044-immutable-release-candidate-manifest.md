# ADR-044：不可变发布候选清单

状态：Accepted（2026-07-16）。

## 决策

发布候选冻结 `git_sha`、镜像/SBOM digest、迁移、Prompt/检索/模型路由版本、数据集、评测运行、已知问题和回滚目标，并计算规范化清单 SHA-256。候选创建后不得原地修改；任何制品或配置变化都创建新候选。

## 理由与后果

这保证 UAT、审批和灰度观察指向同一制品，避免“测的是 A，上线的是 B”。存储 digest 不等于镜像已签名；生产仍需可信 registry、SBOM/签名验证和 provenance。
