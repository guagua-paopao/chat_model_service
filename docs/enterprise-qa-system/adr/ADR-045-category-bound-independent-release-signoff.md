# ADR-045：按类别绑定的独立发布签署

状态：Accepted（2026-07-16）。

## 决策

产品、业务、数据、安全、SRE 五类签署分别绑定服务端角色；同一类别和同一签署人在一个候选上只能写一次，候选创建者不得自签。签署记录不可变，拒绝会终止候选。

## 理由与后果

审批 ID、证据引用、理由与签署人进入审计链，降低口头批准和代签风险。开发 persona 只用于合成演练，不能替代企业 IAM 与具名签字。
