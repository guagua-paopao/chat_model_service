# ADR-046：单调递进的灰度阶段

状态：Accepted（2026-07-16）。

## 决策

灰度顺序固定为 `dark → 5% → 25% → 50% → 100%`，禁止跳级或倒退。每次前进必须提交前一阶段的有界观察窗口；回退只能通过显式 rollback。

## 理由与后果

单调状态机使证据和责任边界可审计，避免绕过观察窗口。生产流量切分由外部部署控制器执行，本地 API 只演练决策语义。
