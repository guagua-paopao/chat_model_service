# ADR-047：服务端灰度停止与回滚门禁

状态：Accepted（2026-07-16）。

## 决策

错误率、TTFT、完整延迟、负反馈、引用精度、成本差异、质量差异、安全事件和越权泄漏阈值由服务端持有。普通退化自动停止；安全事件或越权泄漏自动回滚。

## 理由与后果

客户端不得通过降低阈值强行放量。当前观察值来自合成演练，生产必须由批准的 Prometheus/APM/质量平台签名或可信抓取。
